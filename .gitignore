# Secrets — never commit
.env
.env.*
!.env.example

# Python
.venv/
__pycache__/
*.py[cod]

# Node
node_modules/
package-lock.json

# Eval run output — regenerated whenever the prompt changes.
# dataset.json IS committed (it's the stable baseline); these are derived from it.
answers.json
report_*.html
